Image AI Telegram Bot — Final V8

Source reviewed against the public MrinalBae/Nw repository and corrected before packaging.

Critical corrections:
- Removed the invalid get_temp_file import from app.database in main.py.
- /media now reads temporary image bytes from the filesystem safely.
- Image processing no longer accesses a nonexistent record["data"] field.
- Custom Expand dimensions now correctly proceed to side selection and scale selection.
- Ratio Expand validates side compatibility instead of silently changing the requested target.
- Temporary file IDs are path-safe and expired files are removed.
- Multiple Pixelcut API records have no fixed 10-key limit in database retrieval.
- Render runs FastAPI directly on 0.0.0.0:$PORT.
- No React/Vite/Node/npm/pnpm build artifacts are included.

Static verification performed:
- 19 Python source files parsed and compiled successfully.
- Required files verified.
- No .pyc or __pycache__ files packaged.
- ZIP integrity test passed.
- Forbidden frontend build commands absent from application source.
- Pixelcut endpoints/request format checked against current official Pixelcut documentation.

Live third-party E2E testing cannot be honestly claimed without the owner's Telegram token, MongoDB URI and Pixelcut API credentials.
